import tw from "twin.macro";
import styled from "@emotion/styled";
const Background =
  styled.div(tw`bg-gradient-to-b from-gray-500 to-gray-300 absolute w-full h-full
`);

export default Background;
