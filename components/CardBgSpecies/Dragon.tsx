import tw from "twin.macro";
import styled from "@emotion/styled";
const Background =
  styled.div(tw`bg-gradient-to-b from-red-700 via-gray-700 to-gray-900 absolute w-full h-full
`);

export default Background;
