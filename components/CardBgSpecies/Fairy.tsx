import tw from "twin.macro";
import styled from "@emotion/styled";
const Background =
  styled.div(tw`bg-gradient-to-b from-blue-400 to-green-100 absolute w-full h-full
`);

export default Background;
