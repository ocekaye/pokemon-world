import tw from "twin.macro";
import styled from "@emotion/styled";
const Background =
  styled.div(tw`bg-gradient-to-b from-red-600 via-red-600 to-yellow-400 absolute w-full h-full
`);

export default Background;
