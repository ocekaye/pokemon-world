import tw from "twin.macro";
import styled from "@emotion/styled";
const Background =
  styled.div(tw`bg-gradient-to-b from-yellow-700 via-yellow-400 to-yellow-400 absolute w-full h-full
`);

export default Background;
