import tw from "twin.macro";
import styled from "@emotion/styled";
const Background =
  styled.div(tw`bg-gradient-to-b from-yellow-900 to-yellow-700 absolute w-full h-full
`);

export default Background;
