import tw from "twin.macro";
import styled from "@emotion/styled";
const Background =
  styled.div(tw`bg-gradient-to-b from-pink-700 to-pink-500 absolute w-full h-full
`);

export default Background;
