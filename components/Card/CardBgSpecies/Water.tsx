import tw from "twin.macro";
import styled from "@emotion/styled";
const Background =
  styled.div(tw`bg-gradient-to-b from-blue-600 via-blue-400 to-green-300 absolute w-full h-full
`);

export default Background;
