import tw from "twin.macro";
import styled from "@emotion/styled";
const Background =
  styled.div(tw`bg-gradient-to-b from-gray-900 to-gray-600 absolute w-full h-full
`);

export default Background;
