import tw from "twin.macro";
import styled from "@emotion/styled";
const Background =
  styled.div(tw`bg-gradient-to-b from-gray-400 to-gray-700 absolute w-full h-full animate-pulse
`);

export default Background;
