import tw from "twin.macro";
import styled from "@emotion/styled";
const Background =
  styled.div(tw`bg-gradient-to-b from-blue-600 to-green-500 absolute w-full h-full
`);

export default Background;
