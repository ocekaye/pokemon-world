import tw from "twin.macro";
import styled from "@emotion/styled";
const Background =
  styled.div(tw`bg-gradient-to-b from-green-700 via-green-600 to-green-400 fixed w-full h-full
`);

export default Background;
