import tw from "twin.macro";
import styled from "@emotion/styled";
const Background =
  styled.div(tw`bg-gradient-to-b from-green-900 via-green-500 to-yellow-400 absolute w-full h-full
`);

export default Background;
