import tw from "twin.macro";
import styled from "@emotion/styled";

const DetailInfoContainer = styled.div(tw`
    flex 
    flex-col w-full  
    bg-gray-300 
    bg-opacity-40 
    rounded-xl 
`);

export default DetailInfoContainer;
