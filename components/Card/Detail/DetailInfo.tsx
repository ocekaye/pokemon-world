import tw from "twin.macro";
import styled from "@emotion/styled";

const Info = styled.div(tw`
    flex 
    flex-col 
    gap-2
    items-center 
    flex-grow 
    w-11/12
    sm:w-auto
    m-4
    self-start
`);

export default Info;
